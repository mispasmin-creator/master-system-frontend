/**
 * DEPRECATED — this file was the standalone Vite app's Supabase client.
 *
 * The merged app uses the new REST backend via `systems/freight-payment/lib/api.ts`.
 * All references to `api.*`, `supabase`, `purchaseSupabase`, and `orderSupabase`
 * have been migrated to `freightPaymentApi` from `lib/api.ts`.
 *
 * This file is intentionally kept as an empty stub so that any stale import
 * does not crash the build while the migration is in progress.
 */

export {};
